#include <bits/stdc++.h>

using namespace std;

int main()
{
    long long n;
    cin>>n;
    if(n<=3)
    {
        cout<<1;
        return 0;
    }

    for(long long i=3 ;true; i*=3)
    {
        if(n%i)
        {
            cout<<n/i+1;
            break;
        }
    }
    return 0;
}